type ConfigurationTypes = 
 variant {
   boolean;
   number;
   string;
 };
service : {
  createApplication: (text, text) -> (text);
  createConfiguration: (text, text, text, ConfigurationTypes) -> (text);
  createEnvironment: (text, text, text) -> (text);
  get: (text) -> (opt text);
  set: (text, text) -> ();
}
